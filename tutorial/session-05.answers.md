# Answers

## Concept check

1. They would trigger the cpu exceptions 0-15 if the real-time-clock triggered an interrupt 
   the isr for a double fault would run.

2. Because a slave irq goes 'through' both chips.

3. The interrupt handler gets only called once. The PIC does not know that the interrupt 
   handling is done.

## Mutation exercise


